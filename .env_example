# Requerida
GEMINI_API_KEY=

# Requerida — credenciales de la cuenta de servicio de Google Sheets, en base64
# (JSON de la service account codificado, ej. PowerShell:
#  [Convert]::ToBase64String([IO.File]::ReadAllBytes("key.json")))
MKP_INVENTORY_BASE64=

# Opcionales (defaults en src/config/settings.py)
# COUNTRY=MX
# GEMINI_MODEL=gemini-3-pro-preview
